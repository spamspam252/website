mixin line
	.line
		.round

mixin linex(id)
	div(id=id)
		.line
			.round

mixin sectionHeader(header)
	.sectionHeader
		a(href="#")
			.button-fill
				.button-text= header
				.button-inside
					.inside-text Give&nbspMe&nbspMore

mixin articleContent(blogImgSrc, title, time, bodyText)
	article.Article
		.BlogImage
			img(src= blogImgSrc, alt="Nope")
		section.ArticleText
			h2= title
			small= time
			p= bodyText
		
